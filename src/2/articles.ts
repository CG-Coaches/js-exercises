import { Article } from './models/article.model';
import { ArticleSummary } from './models/article-summary.model';

export function getSimpleExcerpt(body: string): string {
  return ''; // TODO fix this function to return this first 100 characters and then '...'
}
export function getArticleSummaries(articles: Article[]): ArticleSummary[] {
  // TODO implement this function to return article summaries
  return [];
}
